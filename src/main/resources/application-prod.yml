server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_booking_prod
    username: prod_user
    password: prod_pass

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false