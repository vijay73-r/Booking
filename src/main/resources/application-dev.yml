server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_booking_dev
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true